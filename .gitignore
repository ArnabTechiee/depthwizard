.venv/
__pycache__/
*.pyc

# Large and re-fetchable via scripts/fetch_maxar.py and The National Map
data/raw/
data/lidar/
*.laz

# Pipeline intermediates - regenerate with the commands in the README.
# The JSON files beside them (buildings.json, validation.json, ingest.json)
# ARE committed: they are small and hold the results the team cites.
data/work/*/*.npy

# Colab round-trip artefacts
*__rgb.npy
*__depth.npy
depth_outputs.zip
depthout/

# Vendored at build time, not source
viewer/vendor/
outputs/
